/* Algoritimo */
/**  
 * 1.Declare uma constante fatorial e atribua o valor a ser calculado
 *    2.Declare uma varialvel number e atribua o valor da constante fatorial
 *    3.Verifique se o valor do fatorial é negativo
 *    4.Caso o valor do fatorial seja negativo, imprima uma mensagem de erro informando o usuario
 *    5.Caso o fatorial seja igual a 0 ou 1, imprima uma mensagem informando que o fatorial é igual a 1
 *    6.Caso contrario, faça uma repetição inicializando com uma variavel contendo o valor 
 *    na variavel number e até que o valor seja maior que 1
 *    7.Para cada repetição atribua a number o valor de number vezes a variavel de inicialização -1 
 *    8.Imprima o fatorial
*/

/* Segue o codigo abaixo */

/*
const fatorial = 0;
let number = fatorial;

if (fatorial < 0) {
  console.log('O numero para o calculo do fatorial deve ser positivo');
} else if (fatorial === 0 || fatorial === 1) {
  console.log('O fatorial de ' + fatorial + ' é 1');
} else {
  for (let i = number; i > 1; i--) {
    number = number * (i - 1);
  }
  console.log('O fatorial de ' + fatorial + ' é ' + number);
}
*/
